import streamlit as st
st.set_page_config(page_title="Land Scout Lite", layout="wide")
st.title("Land Scout Lite")
st.write("✅ Repo is wired and Streamlit runs. Next steps: add ingest + enrichment scripts and load CSVs.")
st.write("Drop a deals CSV (with columns: listing_id, county, acres, price, estimated_value, etc.) to preview.")
